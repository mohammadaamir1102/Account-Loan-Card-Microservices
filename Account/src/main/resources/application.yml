spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password: ''
    driver-class-name : org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080

build:
  version: "1.0"

accounts:
  message: "Welcome to KhanBank accounts related local APIs "
  contactDetails:
    name: "Aamir Khan - Developer"
    email: "aamir@khanbank.com"
  onCallSupport:
    - (333) 777-8765
    - (333) 888-6543