spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password: ''
    driver-class-name : org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
  config:
    import:
      - "application_qa.yml"
      - "application_prod.yml"
  profiles:
    active:
      - "qa"

server:
  port: 8080

build:
  version: "3.0"

accounts:
  message: "Welcome to KhanBank accounts related local APIs "
  contactDetails:
    name: "Aaquib Khan - Developer"
    email: "aaquib@khanbank.com"
  onCallSupport:
    - (333) 777-8765
    - (333) 888-6543